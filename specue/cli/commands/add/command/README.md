<dec-body>
`specue add <path>` — заводит решение по пути с заранее созданными файлами, проверяя целостность модуля.

Аргументы:
- `<path>` — путь к будущей папке решения внутри модуля (обязателен);
- `--format` — вид вывода 
</dec-body>
