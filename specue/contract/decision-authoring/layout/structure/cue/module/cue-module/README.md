<dec-body>

Контейнер решений — это CUE-модуль с именем и версией. Идентификатор модуля —
это имя и версия вместе: `module@version`.

Модуль объявляет себя в `cue.mod/module.cue`:

```cue
module: "specue.io/models/decisioning@v0"
language: {
    version: "v0.17.0"
}
```

Версия обязательна и фиксирована: другие модули опираются на этот модуль по
конкретной версии. 
</dec-body>
