<dec-body>

Схема specue поддерживается на CUE **v0.17.0** и выше.

Каждый модуль решений объявляет `language.version` в `cue.mod/module.cue`.

Ниже `v0.17.0` нет `local-module.cue` replace, на котором держится локальный
авторинг
</dec-body>
