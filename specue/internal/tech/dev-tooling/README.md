<dec-body>

Тулчейн и переиспользуемые dev-таски ведутся через **mise** (`mise.toml`).

- `[tools]` пиннит версии зависимостей — воспроизводимое окружение
  (`mise install`);

- `[env]` задаёт общие переменные
- `[tasks.*]` — переиспользуемые команды, запускаются `mise run <имя>` 
</dec-body>
