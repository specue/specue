<dec-body>

Модули хранятся в CUE-реестре (OCI). Версии разрешаются через него:
`CUE_REGISTRY` указывает, какой реестр использовать.

Локально — встроенный/локальный реестр (например `localhost:5005+insecure`),
без внешних сервисов. CUE-модули нативно работают с такими реестрами.
</dec-body>
