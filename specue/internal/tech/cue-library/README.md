<dec-body>

Работа с CUE — через официальный Go-модуль `cuelang.org/go`:
`cue`, `cue/load`, `cue/cuecontext`.

Это эталонная реализация CUE: загрузка пакетов, вычисление значений,
валидация. 
</dec-body>
