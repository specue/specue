<dec-body>
Точка входа — `main.go` в корне модуля: тонкий враппер, который
делегирует в пакет `cmd` (корневая cobra-команда) и возвращает её
exit-код. Своей логики не несёт.

Бинарь собирается из корня модуля; имя бинаря — отдельно от каталога
графа, чтобы не конфликтовать.
</dec-body>
