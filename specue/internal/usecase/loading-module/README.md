<dec-body>
Загрузка модуля от его корня (корень находит resolving):

1. загрузить все пакеты модуля средствами CUE (`./...`). Не грузится
   (битый `module.cue`, ошибки) — отказ `invalid_module`;
2. в зависимостях модуля есть `specue.io/schema`. Нет — отказ
   `schema_missing`;
3. в каждом пакете взять поле `decision` — это решение;
4. подтянуть тело из README.md рядом со spec.cue;
5. разложить поля решения (problem, status, context) и связи (links);
6. собрать всё в граф: узлы-решения, рёбра по связям.
</dec-body>
