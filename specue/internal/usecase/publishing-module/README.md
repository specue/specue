<dec-body>

Чтобы переиспользовать модуль решений между командами, его публикуют в
общий (удалённый) реестр — командой из корня модуля (где лежит `cue.mod/`):

```
cd <модуль> && cue mod publish v0.0.1
```

Публикуется текущий модуль под указанной версией. 
</dec-body>
